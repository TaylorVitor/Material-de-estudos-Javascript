
/*
let spaceShip = "Helmet"
let newSpaceShip = ""

for(let i = 0; i < spaceShip.length; i++){
    if(spaceShip[i] == "e" ){
        newSpaceShip += "i"
    }else {
        newSpaceShip += spaceShip[i]
    }
}

console.log(newSpaceShip)
*/


let spaceShip = "instituetion"
let newSpaceShip = ""

for(let i = 0 ; i < spaceShip.length; i++){
    if(spaceShip[i] === "i"){
        newSpaceShip += "e"

    }else{
        newSpaceShip += spaceShip[i]
    }
}

console.log(newSpaceShip)