

/*
let velocity = 50
let acelaration =  5

while(velocity <= 100){
    console.log("Acelerando: estamos a " + velocity)
    velocity += acelaration
}

-----------------------------------------------------------------------

let constellation = "Andromeda"
let pos = 0
let constellationLength = constellation.length

while(pos < constellation.length){
    if(constellation [pos] === "a" || constellation [pos] === "A"){
        console.log(pos)
    }
    pos += 1
}


let fruit = "Abobora"
let pos = 0
let fruitLength = fruit.length

while(pos < fruit.length){
    if(fruit [pos] === "A" || fruit [pos] === "a"){
        console.log(pos)
    }
    pos += 1
}


**/ 

let myName = "Taylor"
let pos = 0
let myNameLength = myName.length

while(pos < myName.length){
    if(myName [pos] === "T" || myName [pos] === "r"){
        console.log(pos)
    }
    pos += 1
}
